@{ 

    string str = "";
    char[] str2 = ViewBag.str1.ToCharArray();

    for (int i = ViewBag.str1.Length - 1; i >= 0; i--)
    {
        str = str + str2[i].ToString();
    }

    str = str.Substring(ViewBag.a, ViewBag.b);
}

Исходная строка: @ViewBag.str1
<br />
Перевернутая строка: @str